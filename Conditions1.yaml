- name: varaibles11
  hosts: localhost
  vars_prompt:
  - name: number
    propmt: enter number
    private: no
  tasks:
  - name: greater number
    ansible.builtin.debug:
    msg: "nmuber {{number}} is greterthan 10"
  when: number > 10